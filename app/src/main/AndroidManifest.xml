<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="fyp.cms">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps"></activity>
        <activity
            android:name=".Admin_Home"
            android:label="@string/title_activity_admin__home"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".add_animals"
            android:label="@string/title_activity_add_animals"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat" />
        <activity
            android:name=".buyer_home_page"
            android:label="@string/title_activity_buyer_home_page"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".sellar_home_page"
            android:label="@string/title_activity_sellar_home_page"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Selection"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".shopping_cart"
            android:label="Shopping Cart"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity android:name=".Payment_Page"></activity>
        <activity android:name=".billing" />
        <activity android:name=".orderDetails" android:label="Order Details"
            android:theme="@style/AppTheme.NoActionBar"/>
        <activity android:name=".orders_list_page" android:label="Orders"
            android:theme="@style/AppTheme.NoActionBar" />
    </application>

</manifest>